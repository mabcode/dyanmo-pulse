// Player camera can be reassigned to other objects	if needed
target = obj_player;
width = camera_get_view_width(view_camera[0]);
height = camera_get_view_height(view_camera[0]);

healthBarW = 200;
healthBarH = 30;

healthBarX = xstart -90;
healthBarY = ystart -150;
